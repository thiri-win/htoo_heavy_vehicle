import { useEffect, useMemo, useState } from 'react'
import { Bar, BarChart, CartesianGrid, Cell, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Bell, CarFront, ChevronLeft, ChevronRight, ChevronsLeft, ChevronsRight, CircleDollarSign, ClipboardList, Download, Eye, FileText, LayoutDashboard, LogOut, Menu, Moon, Pencil, Plus, Printer, Save, Search, Settings, ShieldCheck, Sun, Trash2, UserCircle, Users, X } from 'lucide-react'
import { api, type Car, type Customer, type Invoice, type InvoiceDetail, type Item, type User } from './lib/api'
import { initials, money, shortDate, cn } from './lib/utils'
import { Badge, Button, Card, Dialog, EmptyState, Input, Select } from './components/ui'

type PageName = 'dashboard' | 'invoices' | 'invoice-create' | 'invoice-edit' | 'invoice-detail' | 'customers' | 'customer-detail' | 'cars' | 'car-detail' | 'items' | 'item-detail' | 'team-access' | 'settings'
type Toast = { message: string; kind?: 'success' | 'error' }
type Language = 'en' | 'my'
type ThemeMode = 'light' | 'dark'
type PaymentType = 'pending' | 'kbz-pay' | 'cash'
type PrintDesign = 'classic' | 'minimal' | 'accent'
type PrintOptions = { header: string; subtitle: string; address: string; phone: string; accent: string; design: PrintDesign }

const paymentTypeOptions: Array<{ value: PaymentType; label: string }> = [
  { value: 'pending', label: 'pending' }, { value: 'kbz-pay', label: 'kbz-pay' }, { value: 'cash', label: 'cash' },
]

const navItems: Array<{ id: PageName; labelKey: string; icon: typeof LayoutDashboard }> = [
  { id: 'dashboard', labelKey: 'overview', icon: LayoutDashboard }, { id: 'invoices', labelKey: 'invoices', icon: FileText }, { id: 'customers', labelKey: 'customers', icon: Users }, { id: 'cars', labelKey: 'vehicles', icon: CarFront }, { id: 'items', labelKey: 'services', icon: ClipboardList },
]

const translations: Record<Language, Record<string, string>> = {
  en: { overview: 'Overview', invoices: 'Invoices', customers: 'Customers', vehicles: 'Vehicles', services: 'Services & Items', settings: 'Settings', mainMenu: 'Main menu', workspace: 'Workspace', quickTip: 'Quick tip', signOut: 'Log out', profile: 'User profile', profileDescription: 'Update the name and email shown in your workspace.', appearance: 'Appearance', appearanceDescription: 'Choose how HTOO looks on this device.', language: 'Language', languageDescription: 'Choose the language used by the workspace.', theme: 'Theme', light: 'Light', dark: 'Dark', english: 'English', myanmar: 'Myanmar', saveChanges: 'Save changes', preferencesSaved: 'Preferences saved', financialRecords: 'Financial records', invoiceIndex: 'Invoice index', invoiceIndexDescription: 'Search and manage every service invoice.', invoiceDetail: 'Invoice detail', newInvoice: 'New invoice', createInvoice: 'Create invoice', editInvoice: 'Edit invoice', createInvoiceDescription: 'Add a new service invoice to your live records.', editInvoiceDescription: 'Update the service, vehicle, and payment details.', backToInvoices: 'Back to invoices', cancel: 'Cancel', saveInvoice: 'Save invoice', exportExcel: 'Export Excel', searchInvoices: 'Search invoice, customer, vehicle…', allStatuses: 'All statuses', paid: 'Paid', partial: 'Partial', pending: 'Pending', invoiceDate: 'Invoice date', paymentStatus: 'Payment status', customer: 'Customer', vehicleNumber: 'Vehicle number', brand: 'Brand', model: 'Model', serviceItems: 'Service items', addItem: 'Add item', serviceDescription: 'Service description', advanceReceived: 'Advance received', balanceDue: 'Balance due', subtotal: 'Subtotal', advance: 'Advance', grandTotal: 'Grand total', goodMorning: 'Good morning', dashboardDescription: 'Here is what’s happening with your workshop today.', totalRevenue: 'Total revenue', openBalance: 'Open balance', activeCustomers: 'Active customers', vehiclesServiced: 'Vehicles serviced', fromLiveInvoices: 'From live invoices', liveCustomerRecords: 'Live customer records', liveVehicleRecords: 'Live vehicle records', invoiceActivity: 'Invoice activity', paymentStatusChart: 'Payment status', recentInvoices: 'Recent invoices', revenueByMonth: 'Revenue by month', viewAll: 'View all →', printPdf: 'Print / Save PDF', printSetup: 'PDF print setup', printSetupDescription: 'Add the header and design details before printing.', pdfHeader: 'Header', pdfSubtitle: 'Subtitle', pdfAddress: 'Address', pdfPhone: 'Phone', pdfDesign: 'Design', classic: 'Classic', minimal: 'Minimal', accent: 'Accent', print: 'Print / Save PDF', close: 'Close', billTo: 'Bill to', vehicle: 'Vehicle', serviceDescriptionColumn: 'Service description', quantity: 'Qty', price: 'Price', total: 'Total' },
  my: { overview: 'ခြုံငုံသုံးသပ်ချက်', invoices: 'ငွေတောင်းခံလွှာများ', customers: 'ဖောက်သည်များ', vehicles: 'ယာဉ်များ', services: 'ဝန်ဆောင်မှုနှင့်ပစ္စည်းများ', settings: 'ဆက်တင်များ', mainMenu: 'အဓိကမီနူး', workspace: 'လုပ်ငန်းခွင်', quickTip: 'အမြန်အကြံပြုချက်', signOut: 'ထွက်ရန်', profile: 'အသုံးပြုသူပရိုဖိုင်', profileDescription: 'လုပ်ငန်းခွင်တွင်ပြသမည့် အမည်နှင့်အီးမေးလ်ကို ပြင်ဆင်ပါ။', appearance: 'အသွင်အပြင်', appearanceDescription: 'ဤစက်တွင် HTOO ပြသပုံကို ရွေးချယ်ပါ။', language: 'ဘာသာစကား', languageDescription: 'လုပ်ငန်းခွင်တွင် အသုံးပြုမည့်ဘာသာစကားကို ရွေးပါ။', theme: 'အရောင်ပုံစံ', light: 'အလင်း', dark: 'အမှောင်', english: 'အင်္ဂလိပ်', myanmar: 'မြန်မာ', saveChanges: 'ပြောင်းလဲမှုများ သိမ်းရန်', preferencesSaved: 'စိတ်ကြိုက်ရွေးချယ်မှုများ သိမ်းပြီးပါပြီ', financialRecords: 'ငွေကြေးမှတ်တမ်းများ', invoiceIndex: 'ငွေတောင်းခံလွှာစာရင်း', invoiceIndexDescription: 'ဝန်ဆောင်မှုငွေတောင်းခံလွှာများကို ရှာဖွေစီမံပါ။', invoiceDetail: 'ငွေတောင်းခံလွှာအသေးစိတ်', newInvoice: 'ငွေတောင်းခံလွှာအသစ်', createInvoice: 'ငွေတောင်းခံလွှာ ဖန်တီးရန်', editInvoice: 'ငွေတောင်းခံလွှာ ပြင်ရန်', createInvoiceDescription: 'တိုက်ရိုက်မှတ်တမ်းများထဲသို့ ဝန်ဆောင်မှုငွေတောင်းခံလွှာအသစ် ထည့်ပါ။', editInvoiceDescription: 'ဝန်ဆောင်မှု၊ ယာဉ်နှင့် ငွေပေးချေမှုအသေးစိတ်ကို ပြင်ဆင်ပါ။', backToInvoices: 'ငွေတောင်းခံလွှာများသို့ ပြန်ရန်', cancel: 'ပယ်ဖျက်ရန်', saveInvoice: 'ငွေတောင်းခံလွှာ သိမ်းရန်', exportExcel: 'Excel ထုတ်ရန်', searchInvoices: 'ငွေတောင်းခံလွှာ၊ ဖောက်သည်၊ ယာဉ် ရှာရန်…', allStatuses: 'အခြေအနေအားလုံး', paid: 'ပေးချေပြီး', partial: 'တစ်စိတ်တစ်ပိုင်း', pending: 'ဆိုင်းငံ့', invoiceDate: 'ငွေတောင်းခံလွှာရက်စွဲ', paymentStatus: 'ငွေပေးချေမှုအခြေအနေ', customer: 'ဖောက်သည်', vehicleNumber: 'ယာဉ်နံပါတ်', brand: 'အမျိုးအစား', model: 'မော်ဒယ်', serviceItems: 'ဝန်ဆောင်မှုများ', addItem: 'ပစ္စည်းထည့်ရန်', serviceDescription: 'ဝန်ဆောင်မှုဖော်ပြချက်', advanceReceived: 'ကြိုတင်လက်ခံရရှိငွေ', balanceDue: 'ကျန်ငွေ', subtotal: 'စုစုပေါင်းခွဲ', advance: 'ကြိုတင်ငွေ', grandTotal: 'စုစုပေါင်း', goodMorning: 'မင်္ဂလာနံနက်ခင်းပါ', dashboardDescription: 'ယနေ့ သင့်အလုပ်ရုံတွင် ဖြစ်ပျက်နေသည်များကို ဤနေရာတွင် ကြည့်နိုင်ပါသည်။', totalRevenue: 'စုစုပေါင်းဝင်ငွေ', openBalance: 'ကျန်ရှိငွေ', activeCustomers: 'လက်ရှိဖောက်သည်များ', vehiclesServiced: 'ဝန်ဆောင်မှုပေးထားသောယာဉ်များ', fromLiveInvoices: 'တိုက်ရိုက်ငွေတောင်းခံလွှာများမှ', liveCustomerRecords: 'တိုက်ရိုက်ဖောက်သည်မှတ်တမ်းများ', liveVehicleRecords: 'တိုက်ရိုက်ယာဉ်မှတ်တမ်းများ', invoiceActivity: 'ငွေတောင်းခံလွှာ လှုပ်ရှားမှု', paymentStatusChart: 'ငွေပေးချေမှုအခြေအနေ', recentInvoices: 'လတ်တလောငွေတောင်းခံလွှာများ', revenueByMonth: 'လစဉ်ဝင်ငွေ', viewAll: 'အားလုံးကြည့်ရန် →', printPdf: 'ပုံနှိပ် / PDF သိမ်းရန်', printSetup: 'PDF ပုံနှိပ်ပြင်ဆင်မှု', printSetupDescription: 'ပုံနှိပ်မထုတ်မီ ခေါင်းစီးနှင့် ဒီဇိုင်းအသေးစိတ် ထည့်ပါ။', pdfHeader: 'ခေါင်းစီး', pdfSubtitle: 'စာတန်းထိုး', pdfAddress: 'လိပ်စာ', pdfPhone: 'ဖုန်း', pdfDesign: 'ဒီဇိုင်း', classic: 'ရိုးရိုး', minimal: 'ရိုးရှင်း', accent: 'အရောင်', print: 'ပုံနှိပ် / PDF သိမ်းရန်', close: 'ပိတ်ရန်', billTo: 'ငွေတောင်းခံသူ', vehicle: 'ယာဉ်', serviceDescriptionColumn: 'ဝန်ဆောင်မှုဖော်ပြချက်', quantity: 'အရေအတွက်', price: 'စျေးနှုန်း', total: 'စုစုပေါင်း' },
}

function t(language: Language, key: string) { return translations[language][key] || translations.en[key] || key }

function normalizePaymentType(value: string | null | undefined): PaymentType {
  const normalized = (value || '').toLowerCase().replace('_', '-')
  if (normalized === 'cash' || normalized === 'paid') return 'cash'
  if (normalized === 'kbz-pay' || normalized === 'partial') return 'kbz-pay'
  return 'pending'
}

function StatusBadge({ status }: { status: string }) {
  const paymentType = normalizePaymentType(status)
  const tone = paymentType === 'cash' ? 'success' : paymentType === 'kbz-pay' ? 'info' : 'warning'
  return <Badge tone={tone}>{paymentType}</Badge>
}

function App() {
  const [user, setUser] = useState<User | null>(() => { try { return JSON.parse(localStorage.getItem('htoo_user') || 'null') } catch { return null } })
  const [page, setPage] = useState<PageName>('dashboard')
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const [sidebarCollapsed, setSidebarCollapsed] = useState(() => localStorage.getItem('htoo_sidebar_collapsed') === 'true')
  const [theme, setTheme] = useState<ThemeMode>(() => localStorage.getItem('htoo_theme') === 'dark' ? 'dark' : 'light')
  const [language, setLanguage] = useState<Language>(() => localStorage.getItem('htoo_language') === 'my' ? 'my' : 'en')
  const [customers, setCustomers] = useState<Customer[]>([])
  const [cars, setCars] = useState<Car[]>([])
  const [invoices, setInvoices] = useState<Invoice[]>([])
  const [toast, setToast] = useState<Toast | null>(null)
  const [printInvoice, setPrintInvoice] = useState<Invoice | null>(null)
  const [printConfigOpen, setPrintConfigOpen] = useState(false)
  const [printOptions, setPrintOptions] = useState<PrintOptions>({ header: 'HTOO Heavy Vehicle', subtitle: 'Workshop service invoice', address: 'Yangon, Myanmar', phone: '', accent: '#5c5ce6', design: 'classic' })
  const [selectedInvoice, setSelectedInvoice] = useState<Invoice | null>(null)
  const [selectedCustomer, setSelectedCustomer] = useState<Customer | null>(null)
  const [selectedCar, setSelectedCar] = useState<Car | null>(null)
  const [selectedItem, setSelectedItem] = useState<Item | null>(null)
  const [loadingData, setLoadingData] = useState(false)
  const [dataError, setDataError] = useState('')

  const items = useMemo(() => {
    const itemMap = new Map<string, Item>()
    invoices.forEach((invoice) => invoice.details?.forEach((detail, index) => {
      const item = detail.item
      const name = item?.name || detail.name
      const id = item?.id ?? detail.item_id ?? -(index + 1)
      const key = String(item?.id ?? detail.item_id ?? `name:${name}`)
      if (name && !itemMap.has(key)) itemMap.set(key, { id, name, price: Number(detail.price || item?.price || 0) })
    }))
    return Array.from(itemMap.values())
  }, [invoices])

  const notify = (message: string, kind: Toast['kind'] = 'success') => { setToast({ message, kind }); window.setTimeout(() => setToast(null), 3000) }
  useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('htoo_theme', theme) }, [theme])
  useEffect(() => { document.documentElement.lang = language === 'my' ? 'my' : 'en'; localStorage.setItem('htoo_language', language) }, [language])
  useEffect(() => { localStorage.setItem('htoo_sidebar_collapsed', String(sidebarCollapsed)) }, [sidebarCollapsed])
  useEffect(() => {
    if (!user) return
    setLoadingData(true)
    setDataError('')
    setCustomers([])
    setCars([])
    setInvoices([])
    Promise.allSettled([api.customers(), api.cars(), api.invoices('page=1&limit=1000')]).then(([customersResult, carsResult, invoicesResult]) => {
      if (customersResult.status === 'fulfilled') setCustomers(customersResult.value.data || [])
      if (carsResult.status === 'fulfilled') setCars(carsResult.value.data || [])
      if (invoicesResult.status === 'fulfilled') setInvoices(invoicesResult.value.data || [])
      const failed = [customersResult, carsResult, invoicesResult].find((result) => result.status === 'rejected')
      if (failed?.status === 'rejected') setDataError(failed.reason instanceof Error ? failed.reason.message : 'Unable to load data from the API.')
      setLoadingData(false)
    })
  }, [user])

  const signOut = () => { localStorage.removeItem('htoo_token'); localStorage.removeItem('htoo_user'); setUser(null) }
  const openPrintSetup = (invoice: Invoice) => { setPrintInvoice(invoice); setPrintConfigOpen(true) }
  const printWithOptions = (options: PrintOptions) => { setPrintOptions(options); setPrintConfigOpen(false); window.setTimeout(() => window.print(), 120) }
  const viewInvoice = (invoice: Invoice) => { setSelectedInvoice(invoice); setPage('invoice-detail') }
  const editInvoice = (invoice: Invoice) => { setSelectedInvoice(invoice); setPage('invoice-edit') }
  const deleteInvoice = async (invoice: Invoice) => {
    try {
      await api.deleteInvoice(invoice.id)
      setInvoices((current) => current.filter((item) => item.id !== invoice.id))
      notify('Invoice deleted')
    } catch (requestError) {
      notify(requestError instanceof Error ? requestError.message : 'Could not delete invoice from the live API.', 'error')
    }
  }
  if (!user) return <Login onLoggedIn={(nextUser) => { setUser(nextUser); localStorage.setItem('htoo_user', JSON.stringify(nextUser)) }} />

  const pageTitle = page === 'settings' ? t(language, 'settings') : page === 'team-access' ? 'Team access' : page === 'invoice-create' ? t(language, 'createInvoice') : page === 'invoice-edit' ? t(language, 'editInvoice') : page === 'invoice-detail' ? t(language, 'invoiceDetail') : page === 'customer-detail' ? selectedCustomer?.name || t(language, 'customers') : page === 'car-detail' ? selectedCar?.number || t(language, 'vehicles') : page === 'item-detail' ? selectedItem?.name || t(language, 'services') : t(language, navItems.find((item) => item.id === page)?.labelKey || 'overview')
  const updateUser = (nextUser: User) => { setUser(nextUser); localStorage.setItem('htoo_user', JSON.stringify(nextUser)) }
  return <div className="app-shell">
    <Sidebar page={page} open={sidebarOpen} collapsed={sidebarCollapsed} language={language} user={user} onToggle={() => setSidebarCollapsed((value) => !value)} onNavigate={(next) => { setPage(next); setSidebarOpen(false) }} onSignOut={signOut} />
    <main className="main">
      <header className="topbar"><div className="breadcrumb"><button className="icon-button mobile-menu" onClick={() => setSidebarOpen(true)}><Menu size={19} /></button><span>{t(language, 'workspace')}</span><span>/</span><strong>{pageTitle}</strong></div><div className="topbar-actions"><div className="search-mini"><Search size={14} /><input placeholder="Quick search" onChange={(event) => { if (event.target.value) { setPage('invoices'); } }} /></div><button className="icon-button" aria-label="Notifications"><Bell size={17} /></button><div className="avatar">{initials(user.name)}</div></div></header>
      <div className="content">
        {dataError && <div className="api-error" role="alert">Live API error: {dataError}</div>}
        {loadingData ? <Card><EmptyState title="Loading live data" description="Connecting to the HTOO API…" /></Card> : <>
        {page === 'dashboard' && <Dashboard language={language} userName={user.name} invoices={invoices} customers={customers} cars={cars} onViewInvoices={() => setPage('invoices')} />}
        {page === 'invoices' && <InvoicesPage language={language} invoices={invoices} onCreate={() => { setSelectedInvoice(null); setPage('invoice-create') }} onView={viewInvoice} onEdit={editInvoice} onChange={setInvoices} onPrint={openPrintSetup} notify={notify} />}
        {page === 'invoice-create' && <InvoiceEditorPage language={language} mode="create" initial={null} customers={customers} cars={cars} items={items} invoices={invoices} onChange={setInvoices} onCancel={() => setPage('invoices')} onDone={() => setPage('invoices')} notify={notify} />}
        {page === 'invoice-edit' && <InvoiceEditorPage language={language} mode="edit" initial={selectedInvoice} customers={customers} cars={cars} items={items} invoices={invoices} onChange={setInvoices} onCancel={() => setPage('invoices')} onDone={() => setPage('invoices')} notify={notify} />}
        {page === 'invoice-detail' && selectedInvoice && <InvoiceDetailPage language={language} invoice={selectedInvoice} onBack={() => setPage('invoices')} onEdit={() => setPage('invoice-edit')} onPrint={openPrintSetup} />}
        {page === 'customers' && <CustomersPage customers={customers} onView={(customer) => { setSelectedCustomer(customer); setPage('customer-detail') }} onChange={setCustomers} notify={notify} />}
        {page === 'customer-detail' && selectedCustomer && <CustomerDetailPage language={language} customer={selectedCustomer} cars={cars} invoices={invoices} onBack={() => setPage('customers')} onViewInvoice={viewInvoice} onEditInvoice={editInvoice} onDeleteInvoice={deleteInvoice} onPrintInvoice={openPrintSetup} />}
        {page === 'cars' && <CarsPage cars={cars} customers={customers} onView={(car) => { setSelectedCar(car); setPage('car-detail') }} onChange={setCars} notify={notify} />}
        {page === 'car-detail' && selectedCar && <CarDetailPage language={language} car={selectedCar} invoices={invoices} onBack={() => setPage('cars')} onViewInvoice={viewInvoice} onEditInvoice={editInvoice} onDeleteInvoice={deleteInvoice} onPrintInvoice={openPrintSetup} />}
        {page === 'items' && <ItemsPage items={items} invoices={invoices} onView={(item) => { setSelectedItem(item); setPage('item-detail') }} />}
        {page === 'item-detail' && selectedItem && <ItemDetailPage language={language} item={selectedItem} invoices={invoices} onBack={() => setPage('items')} onViewInvoice={viewInvoice} />}
        {page === 'team-access' && <TeamAccessPage user={user} notify={notify} />}
        {page === 'settings' && <SettingsPage language={language} theme={theme} user={user} onLanguageChange={setLanguage} onThemeChange={setTheme} onUserChange={updateUser} notify={notify} />}
        {printInvoice && <PrintInvoice invoice={printInvoice} options={printOptions} />}
        </>}
      </div>
    </main>
    <PrintSetupDialog open={printConfigOpen} initial={printOptions} onClose={() => setPrintConfigOpen(false)} onPrint={printWithOptions} />
    {toast && <div className={cn('toast', toast.kind === 'error' && 'toast-error')}>{toast.message}</div>}
  </div>
}

function Login({ onLoggedIn }: { onLoggedIn: (user: User) => void }) {
  const [mode, setMode] = useState<'signin' | 'signup'>('signin')
  const [name, setName] = useState('')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(''); setLoading(true); try { if (mode === 'signin') { const result = await api.signIn(email, password); localStorage.setItem('htoo_token', result.token); onLoggedIn({ id: result.user.id, name: result.user.name, email: result.user.email, role_id: result.user.role_id ?? 3 }) } else { const result = await api.signUp(name, email, password); onLoggedIn({ id: result.data.id, name: result.data.name, email: result.data.email, role_id: result.data.role_id ?? 3 }) } } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to connect to the live API.') } finally { setLoading(false) } }
  return <div className="login-page"><div className="login-art"><div className="brand" style={{ color: 'white', padding: 0 }}><div className="brand-mark">H</div><div><strong>HTOO</strong><small style={{ color: '#c2c4ff' }}>Heavy Vehicle</small></div></div><div><h1>Keep every vehicle moving forward.</h1><p>A focused workspace for service invoices, customers, and the vehicles behind your business.</p></div><div className="login-stats"><div><strong>Live</strong><span>API-connected workspace</span></div><div><strong>100%</strong><span>Real records</span></div><div><strong>24/7</strong><span>Service visibility</span></div></div></div><div className="login-form-side"><form className="login-form" onSubmit={submit}><div className="eyebrow">Operations workspace</div><h2>{mode === 'signin' ? 'Welcome back' : 'Create your account'}</h2><p>{mode === 'signin' ? 'Sign in to manage your workshop operations.' : 'Start organizing your fleet service data.'}</p>{error && <div className="login-note" style={{ color: '#ca5967', borderColor: '#f3cdd1', marginTop: 0, marginBottom: 16 }}>{error}</div>}<div className="login-fields">{mode === 'signup' && <div className="form-field"><label>Full name</label><Input value={name} onChange={(event) => setName(event.target.value)} placeholder="Your name" required /></div>}<div className="form-field"><label>Email address</label><Input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@company.com" required /></div><div className="form-field"><label>Password</label><Input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="••••••••" required /></div><Button className="login-submit" disabled={loading}>{loading ? 'Connecting…' : mode === 'signin' ? 'Sign in to dashboard' : 'Create account'}</Button></div><p style={{ textAlign: 'center', marginTop: 22, marginBottom: 0 }}>{mode === 'signin' ? 'New to HTOO?' : 'Already have an account?'} <button type="button" className="card-link" onClick={() => setMode(mode === 'signin' ? 'signup' : 'signin')}>{mode === 'signin' ? 'Create an account' : 'Sign in'}</button></p></form></div></div>
}

function Sidebar({ page, open, collapsed, language, user, onToggle, onNavigate, onSignOut }: { page: PageName; open: boolean; collapsed: boolean; language: Language; user: User; onToggle: () => void; onNavigate: (page: PageName) => void; onSignOut: () => void }) {
  const isAdmin = user.role_id === 1
  return <aside className={cn('sidebar', open && 'open', collapsed && 'collapsed')}><div className="brand"><div className="brand-mark">H</div><div className="brand-copy"><strong>HTOO</strong><small>Heavy Vehicle</small></div><button className="sidebar-toggle icon-button" onClick={onToggle} title={collapsed ? 'Expand sidebar' : 'Collapse sidebar'} aria-label={collapsed ? 'Expand sidebar' : 'Collapse sidebar'}>{collapsed ? <ChevronsRight size={16} /> : <ChevronsLeft size={16} />}</button></div><div className="nav-label">{t(language, 'mainMenu')}</div><nav className="nav">{navItems.map(({ id, labelKey, icon: Icon }) => <button key={id} className={page === id ? 'active' : ''} onClick={() => onNavigate(id)} title={t(language, labelKey)}><Icon size={17} strokeWidth={1.8} /><span>{t(language, labelKey)}</span></button>)}</nav><div className="sidebar-bottom"><div className="nav-label">{t(language, 'workspace')}</div><nav className="nav"><button className={page === 'settings' ? 'active' : ''} onClick={() => onNavigate('settings')} title={t(language, 'settings')}><Settings size={17} strokeWidth={1.8} /><span>{t(language, 'settings')}</span></button>{isAdmin && <button className={page === 'team-access' ? 'active' : ''} onClick={() => onNavigate('team-access')} title="Team access"><ShieldCheck size={17} strokeWidth={1.8} /><span>Team access</span></button>}<button onClick={onSignOut} title={t(language, 'signOut')}><LogOut size={17} strokeWidth={1.8} /><span>{t(language, 'signOut')}</span></button></nav><div className="user-card"><div className="avatar">{initials(user.name)}</div><div className="user-copy" style={{ flex: 1, minWidth: 0 }}><strong>{user.name}</strong><small>{user.email}</small></div></div></div></aside>
}

function Dashboard({ language, userName, invoices, customers, cars, onViewInvoices }: { language: Language; userName: string; invoices: Invoice[]; customers: Customer[]; cars: Car[]; onViewInvoices: () => void }) {
  const total = invoices.reduce((sum, invoice) => sum + Number(invoice.grand_total || 0), 0)
  const paid = invoices.reduce((sum, invoice) => sum + Number(invoice.advance || 0), 0)
  const pending = Math.max(0, total - paid)
  const monthly = Array.from({ length: 6 }, (_, offset) => {
    const date = new Date()
    date.setDate(1)
    date.setMonth(date.getMonth() - (5 - offset))
    const year = date.getFullYear()
    const month = date.getMonth()
    const monthInvoices = invoices.filter((invoice) => { const invoiceDate = new Date(invoice.date); return invoiceDate.getFullYear() === year && invoiceDate.getMonth() === month })
    return { month: date.toLocaleDateString(undefined, { month: 'short' }), value: monthInvoices.length, revenue: monthInvoices.reduce((sum, invoice) => sum + Number(invoice.advance || 0), 0) }
  })
  const paymentMix = [{ name: 'cash', value: invoices.filter((invoice) => normalizePaymentType(invoice.payment_type) === 'cash').length, color: '#5c5ce6' }, { name: 'kbz-pay', value: invoices.filter((invoice) => normalizePaymentType(invoice.payment_type) === 'kbz-pay').length, color: '#78d6b1' }, { name: 'pending', value: invoices.filter((invoice) => normalizePaymentType(invoice.payment_type) === 'pending').length, color: '#f3c65e' }]
  const today = new Date().toLocaleDateString(undefined, { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' })
  return <><div className="page-heading"><div><div className="eyebrow">{today}</div><h1>{t(language, 'goodMorning')}, {userName}.</h1><p>{t(language, 'dashboardDescription')}</p></div><div className="actions"><Button onClick={onViewInvoices}><Plus size={15} /> {t(language, 'newInvoice')}</Button></div></div><div className="stat-grid"><StatCard label={t(language, 'totalRevenue')} value={money(total)} foot={t(language, 'fromLiveInvoices')} icon={<CircleDollarSign size={16} />} tone="purple" /><StatCard label={t(language, 'openBalance')} value={money(pending)} foot="Calculated from live data" icon={<ClipboardList size={16} />} tone="yellow" /><StatCard label={t(language, 'activeCustomers')} value={String(customers.length)} foot={t(language, 'liveCustomerRecords')} icon={<Users size={16} />} tone="mint" /><StatCard label={t(language, 'vehiclesServiced')} value={String(cars.length)} foot={t(language, 'liveVehicleRecords')} icon={<CarFront size={16} />} tone="blue" /></div><div className="dashboard-grid"><Card><div className="card-title"><div><h2>{t(language, 'invoiceActivity')}</h2><p>Number of invoices created over time</p></div><span className="live-label">Live API</span></div><div className="chart">{invoices.length ? <ResponsiveContainer width="100%" height="100%"><LineChart data={monthly} margin={{ top: 8, right: 10, left: -25, bottom: 0 }}><CartesianGrid vertical={false} /><XAxis dataKey="month" axisLine={false} tickLine={false} /><YAxis axisLine={false} tickLine={false} allowDecimals={false} /><Tooltip content={<ChartTooltip label="Invoices" />} /><Line type="monotone" dataKey="value" stroke="#5c5ce6" strokeWidth={3} dot={{ r: 4, fill: '#5c5ce6', strokeWidth: 3, stroke: '#fff' }} activeDot={{ r: 6 }} /></LineChart></ResponsiveContainer> : <EmptyState title="No invoice activity" description="Create an invoice through the live API to see activity here." />}</div></Card><Card><div className="card-title"><div><h2>{t(language, 'paymentStatusChart')}</h2><p>Current invoice collection mix</p></div><span className="live-label">Live API</span></div>{invoices.length ? <div className="donut-wrap"><div style={{ width: 160, height: 190 }}><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={paymentMix} dataKey="value" innerRadius={52} outerRadius={72} paddingAngle={4} stroke="none">{paymentMix.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip content={<ChartTooltip label="Invoices" />} /></PieChart></ResponsiveContainer></div><div className="donut-legend">{paymentMix.map((item) => <div className="legend-row" key={item.name}><span className="legend-dot" style={{ background: item.color }} /><span>{item.name}</span><strong>{item.value}</strong></div>)}</div></div> : <EmptyState title="No payment data" description="Payment status will appear when the API has invoices." />}</Card></div><div className="dashboard-grid"><Card><div className="card-title"><div><h2>{t(language, 'recentInvoices')}</h2><p>The latest work added to your workspace</p></div><button className="card-link" onClick={onViewInvoices}>{t(language, 'viewAll')}</button></div><InvoiceTable invoices={invoices.slice(0, 4)} compact /></Card><Card><div className="card-title"><div><h2>{t(language, 'revenueByMonth')}</h2><p>Collected invoice value from live records</p></div></div><div className="chart" style={{ height: 224 }}>{invoices.length ? <ResponsiveContainer width="100%" height="100%"><BarChart data={monthly} margin={{ top: 8, right: 4, left: -25, bottom: 0 }}><CartesianGrid vertical={false} /><XAxis dataKey="month" axisLine={false} tickLine={false} /><YAxis axisLine={false} tickLine={false} /><Tooltip content={<ChartTooltip label="Collected" />} /><Bar dataKey="revenue" fill="#78d6b1" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer> : <EmptyState title="No revenue data" description="Collected value will appear when the API has invoices." />}</div></Card></div></>
}
function StatCard({ label, value, foot, icon, tone }: { label: string; value: string; foot: string; icon: React.ReactNode; tone: string }) { return <div className="stat-card"><div className="stat-top"><span>{label}</span><span className={`stat-icon ${tone}`}>{icon}</span></div><div className="stat-value">{value}</div><div className="stat-foot">{foot}</div></div> }
function ChartTooltip({ active, payload, label }: { active?: boolean; payload?: Array<{ value: number }>; label: string }) { return active && payload?.length ? <div className="tooltip">{label}<strong>{payload[0].value}</strong></div> : null }

function InvoicesPage({ language, invoices, onCreate, onView, onEdit, onChange, onPrint, notify }: { language: Language; invoices: Invoice[]; onCreate: () => void; onView: (invoice: Invoice) => void; onEdit: (invoice: Invoice) => void; onChange: (value: Invoice[]) => void; onPrint: (invoice: Invoice) => void; notify: (message: string, kind?: Toast['kind']) => void }) {
  const [query, setQuery] = useState(''); const [status, setStatus] = useState('all'); const [page, setPage] = useState(1); const [deleteId, setDeleteId] = useState<number | null>(null); const pageSize = 6
  const filtered = invoices.filter((invoice) => { const text = `${invoice.invoice_number} ${invoice.customer?.name} ${invoice.car?.number} ${invoice.details?.map((detail) => detail.item?.name || detail.name).join(' ')}`.toLowerCase(); return text.includes(query.toLowerCase()) && (status === 'all' || normalizePaymentType(invoice.payment_type) === status) }); const pages = Math.max(1, Math.ceil(filtered.length / pageSize)); const rows = filtered.slice((page - 1) * pageSize, page * pageSize)
  useEffect(() => setPage(1), [query, status])
  const remove = async () => { if (!deleteId) return; try { await api.deleteInvoice(deleteId); onChange(invoices.filter((invoice) => invoice.id !== deleteId)); setDeleteId(null); notify('Invoice deleted') } catch (requestError) { notify(requestError instanceof Error ? requestError.message : 'Could not delete invoice from the live API.', 'error') } }
  return <><div className="page-heading"><div><div className="eyebrow">{t(language, 'financialRecords')}</div><h1>{t(language, 'invoiceIndex')}</h1><p>{t(language, 'invoiceIndexDescription')}</p></div><div className="actions"><Button variant="outline" onClick={async () => { try { const blob = await api.exportInvoices(); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'invoices-report.xlsx'; link.click(); URL.revokeObjectURL(url); notify('Excel report downloaded') } catch { notify('Could not export the live Excel report.', 'error') } }}><Download size={14} /> {t(language, 'exportExcel')}</Button><Button onClick={onCreate}><Plus size={15} /> {t(language, 'newInvoice')}</Button></div></div><div className="toolbar"><div className="toolbar-left"><div className="search-box"><Search size={15} /><Input placeholder={t(language, 'searchInvoices')} value={query} onChange={(event) => setQuery(event.target.value)} /></div><Select value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">{t(language, 'allStatuses')}</option>{paymentTypeOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</Select></div><div className="toolbar-right"><span style={{ fontSize: 11, color: '#8892a4' }}>{filtered.length} records</span></div></div><Card className="table-card"><InvoiceTable invoices={rows} onView={onView} onEdit={onEdit} onDelete={(invoice) => setDeleteId(invoice.id)} onPrint={onPrint} /><Pagination page={page} pages={pages} total={filtered.length} pageSize={pageSize} onPage={setPage} /></Card><Dialog open={Boolean(deleteId)} title="Delete invoice?" description="This action cannot be undone." onClose={() => setDeleteId(null)} footer={<><Button variant="outline" onClick={() => setDeleteId(null)}>Cancel</Button><Button variant="danger" onClick={remove}>Delete invoice</Button></>}><p style={{ color: '#69758a', fontSize: 13 }}>The invoice will be removed from your records. Any related service details will also be deleted.</p></Dialog></>
}

function InvoiceTable({ invoices, compact = false, onView, onEdit, onDelete, onPrint }: { invoices: Invoice[]; compact?: boolean; onView?: (invoice: Invoice) => void; onEdit?: (invoice: Invoice) => void; onDelete?: (invoice: Invoice) => void; onPrint?: (invoice: Invoice) => void }) { return invoices.length ? <div className="table-wrap"><table className="data-table"><thead><tr><th>Invoice</th><th>Customer</th><th>Vehicle</th><th>Date</th><th>Amount</th><th>Status</th>{!compact && <th></th>}</tr></thead><tbody>{invoices.map((invoice) => <tr key={invoice.id}><td><strong>#{invoice.invoice_number}</strong></td><td><div className="td-main"><span className="tiny-avatar">{initials(invoice.customer?.name || 'NA')}</span><strong>{invoice.customer?.name || 'Walk-in customer'}</strong></div></td><td>{invoice.car?.number || '—'}</td><td>{shortDate(invoice.date)}</td><td><strong>{money(invoice.grand_total)}</strong></td><td><StatusBadge status={invoice.payment_type} /></td>{!compact && <td><div className="table-actions"><Button variant="ghost" size="icon" title="View" onClick={() => onView?.(invoice)}><Eye size={14} /></Button><Button variant="ghost" size="icon" title="Print / Save PDF" onClick={() => onPrint?.(invoice)}><Printer size={14} /></Button><Button variant="ghost" size="icon" title="Edit" onClick={() => onEdit?.(invoice)}><Pencil size={14} /></Button><Button variant="ghost" size="icon" title="Delete" onClick={() => onDelete?.(invoice)}><Trash2 size={14} /></Button></div></td>}</tr>)}</tbody></table></div> : <EmptyState title="No invoices found" description="Try adjusting your search or create a new invoice." /> }

type InvoiceLine = { name: string; quantity: number; price: number }
type InvoiceForm = { date: string; customerName: string; carNumber: string; brand: string; model: string; advance: number; paymentType: string; lines: InvoiceLine[] }
function InvoiceEditorPage({ language, mode, initial, customers, cars, items, invoices, onChange, onCancel, onDone, notify }: { language: Language; mode: 'create' | 'edit'; initial: Invoice | null; customers: Customer[]; cars: Car[]; items: Item[]; invoices: Invoice[]; onChange: (value: Invoice[]) => void; onCancel: () => void; onDone: () => void; notify: (message: string, kind?: Toast['kind']) => void }) {
  const save = async (data: InvoiceForm) => {
    const total = data.lines.reduce((sum, line) => sum + Number(line.price || 0) * Number(line.quantity || 0), 0)
    const payload = { date: data.date, customer: { name: data.customerName }, car: { number: data.carNumber, brand: data.brand, model: data.model }, invoice_details: data.lines.map((line) => ({ name: line.name, quantity: Number(line.quantity), price: Number(line.price) })), sub_total: total, advance: Number(data.advance || 0), grand_total: total, payment_type: data.paymentType, payment_date: data.paymentType === 'cash' ? data.date : null }
    try {
      if (mode === 'edit') {
        if (!initial?.id) {
          notify('This invoice could not be identified for editing.', 'error')
          return
        }
        const result = await api.updateInvoice(initial.id, payload)
        onChange(invoices.map((invoice) => invoice.id === initial.id ? { ...result.data, id: initial.id } : invoice))
      } else {
        const result = await api.createInvoice(payload)
        onChange([result.data, ...invoices])
      }
      notify(mode === 'edit' ? 'Invoice updated' : 'Invoice created')
      onDone()
    } catch (requestError) {
      notify(requestError instanceof Error ? requestError.message : 'Could not save invoice to the live API.', 'error')
    }
  }
  return <><div className="page-heading"><div><button className="back-link" onClick={onCancel}>← {t(language, 'backToInvoices')}</button><div className="eyebrow">{t(language, 'financialRecords')}</div><h1>{mode === 'edit' ? t(language, 'editInvoice') : t(language, 'createInvoice')}</h1><p>{mode === 'edit' ? t(language, 'editInvoiceDescription') : t(language, 'createInvoiceDescription')}</p></div></div><Card className="invoice-editor-card"><InvoiceForm language={language} pageMode initial={initial} customers={customers} cars={cars} items={items} onCancel={onCancel} onSave={save} /></Card></>
}

function InvoiceForm({ language, pageMode = false, initial, customers, cars, items, onCancel, onSave }: { language: Language; pageMode?: boolean; initial: Invoice | null; customers: Customer[]; cars: Car[]; items: Item[]; onCancel: () => void; onSave: (data: InvoiceForm) => void }) {
  const [form, setForm] = useState<InvoiceForm>(() => ({ date: initial?.date?.slice(0, 10) || new Date().toISOString().slice(0, 10), customerName: initial?.customer?.name || '', carNumber: initial?.car?.number || '', brand: initial?.car?.brand || '', model: initial?.car?.model || '', advance: Number(initial?.advance || 0), paymentType: normalizePaymentType(initial?.payment_type), lines: initial?.details?.map((detail) => ({ name: detail.item?.name || detail.name || '', quantity: detail.quantity, price: Number(detail.price) })) || [{ name: items[0]?.name || '', quantity: 1, price: Number(items[0]?.price || 0) }] }))
  const total = form.lines.reduce((sum, line) => sum + Number(line.price || 0) * Number(line.quantity || 0), 0)
  const patch = (next: Partial<InvoiceForm>) => setForm((value) => ({ ...value, ...next }))
  const selectCustomer = (name: string) => { const match = customers.find((customer) => customer.name === name); patch({ customerName: name, carNumber: match && cars.find((car) => car.customer_id === match.id)?.number || form.carNumber }) }
  const selectCar = (number: string) => { const match = cars.find((car) => car.number === number); patch({ carNumber: number, brand: match?.brand || '', model: match?.model || '', customerName: match?.customer?.name || form.customerName }) }
  return <><div className="form-grid"><div className="form-field"><label>{t(language, 'invoiceDate')}</label><Input type="date" value={form.date} onChange={(e) => patch({ date: e.target.value })} /></div><div className="form-field"><label>{t(language, 'paymentStatus')}</label><Select value={form.paymentType} onChange={(e) => patch({ paymentType: e.target.value as PaymentType })}>{paymentTypeOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</Select></div><div className="form-field"><label>{t(language, 'customer')}</label><Input list="customer-list" value={form.customerName} onChange={(e) => selectCustomer(e.target.value)} placeholder="Search or add customer" /><datalist id="customer-list">{customers.map((customer) => <option key={customer.id} value={customer.name} />)}</datalist></div><div className="form-field"><label>{t(language, 'vehicleNumber')}</label><Input list="car-list" value={form.carNumber} onChange={(e) => selectCar(e.target.value)} placeholder="e.g. 9H-4821" /><datalist id="car-list">{cars.map((car) => <option key={car.id} value={car.number} />)}</datalist></div><div className="form-field"><label>{t(language, 'brand')}</label><Input value={form.brand} onChange={(e) => patch({ brand: e.target.value })} placeholder="Hino" /></div><div className="form-field"><label>{t(language, 'model')}</label><Input value={form.model} onChange={(e) => patch({ model: e.target.value })} placeholder="500 Series" /></div></div><hr className="form-divider" /><div className="card-title" style={{ marginBottom: 10 }}><div><h2 style={{ fontSize: 13 }}>{t(language, 'serviceItems')}</h2><p>Add one or more billable services.</p></div><Button variant="secondary" size="sm" onClick={() => patch({ lines: [...form.lines, { name: '', quantity: 1, price: 0 }] })}><Plus size={13} /> {t(language, 'addItem')}</Button></div><div className="line-items">{form.lines.map((line, index) => <div className="line-item" key={index}><Input list="item-list" value={line.name} placeholder={t(language, 'serviceDescription')} onChange={(e) => { const next = [...form.lines]; next[index] = { ...line, name: e.target.value }; patch({ lines: next }) }} /><Input type="number" min="1" value={line.quantity} onChange={(e) => { const next = [...form.lines]; next[index] = { ...line, quantity: Number(e.target.value) }; patch({ lines: next }) }} /><Input type="number" min="0" value={line.price} onChange={(e) => { const next = [...form.lines]; next[index] = { ...line, price: Number(e.target.value) }; patch({ lines: next }) }} />{form.lines.length > 1 ? <Button variant="ghost" size="icon" onClick={() => patch({ lines: form.lines.filter((_, lineIndex) => lineIndex !== index) })}><X size={15} /></Button> : <span />}</div>)}<datalist id="item-list">{items.map((item) => <option key={item.id} value={item.name} />)}</datalist></div><hr className="form-divider" /><div className="form-grid"><div className="form-field"><label>{t(language, 'advanceReceived')}</label><Input type="number" min="0" value={form.advance} onChange={(e) => patch({ advance: Number(e.target.value) })} /></div><div className="form-field"><label>{t(language, 'balanceDue')}</label><Input value={money(Math.max(0, total - Number(form.advance)))} readOnly /></div></div><div className="invoice-totals" style={{ marginTop: 18 }}><div className="invoice-total-row"><span>{t(language, 'subtotal')}</span><strong>{money(total)}</strong></div><div className="invoice-total-row"><span>{t(language, 'advance')}</span><strong>- {money(form.advance)}</strong></div><div className="invoice-total-row grand"><span>{t(language, 'grandTotal')}</span><strong>{money(total)}</strong></div></div><div className={pageMode ? 'editor-actions' : 'dialog-footer'} style={pageMode ? undefined : { margin: '20px -22px -20px' }}><Button variant="outline" onClick={onCancel}>{t(language, 'cancel')}</Button><Button onClick={() => onSave(form)} disabled={!form.customerName || !form.carNumber || !form.lines[0]?.name}>{t(language, 'saveInvoice')}</Button></div></>
}

function InvoiceDetailPage({ language, invoice, onBack, onEdit, onPrint }: { language: Language; invoice: Invoice; onBack: () => void; onEdit: () => void; onPrint: (invoice: Invoice) => void }) {
  return <><div className="page-heading"><div><button className="back-link" onClick={onBack}>← {t(language, 'backToInvoices')}</button><div className="eyebrow">{t(language, 'financialRecords')}</div><h1>{t(language, 'invoiceDetail')}</h1><p>#{invoice.invoice_number} · {shortDate(invoice.date)}</p></div><div className="actions"><Button variant="outline" onClick={() => onPrint(invoice)}><Printer size={14} /> {t(language, 'printPdf')}</Button><Button onClick={onEdit}><Pencil size={14} /> {t(language, 'editInvoice')}</Button></div></div><div className="detail-grid"><Card><div className="detail-label">{t(language, 'billTo')}</div><div className="detail-person"><div className="avatar">{initials(invoice.customer?.name || 'NA')}</div><div><h2>{invoice.customer?.name || '—'}</h2><p>{invoice.customer?.phone || 'No phone recorded'}</p></div></div></Card><Card><div className="detail-label">{t(language, 'vehicle')}</div><div className="detail-person"><div className="vehicle-icon"><CarFront size={19} /></div><div><h2>{invoice.car?.number || '—'}</h2><p>{[invoice.car?.brand, invoice.car?.model].filter(Boolean).join(' ') || 'No model recorded'}</p></div></div></Card><Card><div className="detail-label">{t(language, 'paymentStatus')}</div><div className="detail-payment"><StatusBadge status={invoice.payment_type} /><strong>{money(invoice.grand_total)}</strong><span>Advance: {money(invoice.advance)}</span></div></Card></div><Card className="detail-lines-card"><div className="card-title"><div><h2>{t(language, 'serviceItems')}</h2><p>Services included in this invoice.</p></div><StatusBadge status={invoice.payment_type} /></div><div className="table-wrap"><table className="data-table"><thead><tr><th>{t(language, 'serviceDescriptionColumn')}</th><th>{t(language, 'quantity')}</th><th>{t(language, 'price')}</th><th>{t(language, 'total')}</th></tr></thead><tbody>{invoice.details?.map((detail, index) => <tr key={detail.id || index}><td><strong>{detail.item?.name || detail.name || '—'}</strong></td><td>{detail.quantity}</td><td>{money(detail.price)}</td><td><strong>{money(Number(detail.price) * Number(detail.quantity))}</strong></td></tr>)}</tbody></table></div><div className="invoice-totals detail-totals"><div className="invoice-total-row"><span>{t(language, 'subtotal')}</span><strong>{money(invoice.sub_total)}</strong></div><div className="invoice-total-row"><span>{t(language, 'advance')}</span><strong>- {money(invoice.advance)}</strong></div><div className="invoice-total-row grand"><span>{t(language, 'grandTotal')}</span><strong>{money(invoice.grand_total)}</strong></div></div></Card></>
}

function PrintSetupDialog({ open, initial, onClose, onPrint }: { open: boolean; initial: PrintOptions; onClose: () => void; onPrint: (options: PrintOptions) => void }) {
  const [options, setOptions] = useState(initial)
  useEffect(() => { if (open) setOptions(initial) }, [open, initial])
  const patch = (next: Partial<PrintOptions>) => setOptions((value) => ({ ...value, ...next }))
  return <Dialog open={open} title="PDF print setup" description="Add the header and design details before printing." onClose={onClose} footer={<><Button variant="outline" onClick={onClose}>Close</Button><Button onClick={() => onPrint(options)}><Printer size={14} /> Print / Save PDF</Button></>}><div className="form-grid"><div className="form-field full"><label>Header / business name</label><Input autoFocus value={options.header} onChange={(event) => patch({ header: event.target.value })} placeholder="HTOO Heavy Vehicle" /></div><div className="form-field"><label>Subtitle</label><Input value={options.subtitle} onChange={(event) => patch({ subtitle: event.target.value })} placeholder="Workshop service invoice" /></div><div className="form-field"><label>Phone</label><Input value={options.phone} onChange={(event) => patch({ phone: event.target.value })} placeholder="09 000 000 000" /></div><div className="form-field"><label>Address</label><Input value={options.address} onChange={(event) => patch({ address: event.target.value })} placeholder="Yangon, Myanmar" /></div><div className="form-field"><label>Accent color</label><Input type="color" value={options.accent} onChange={(event) => patch({ accent: event.target.value })} style={{ height: 38, padding: 4 }} /></div><div className="form-field full"><label>Print design</label><Select value={options.design} onChange={(event) => patch({ design: event.target.value as PrintDesign })}><option value="classic">Classic — structured business invoice</option><option value="minimal">Minimal — clean black and white</option><option value="accent">Accent — branded color header</option></Select></div></div><div className="print-preview-note"><Printer size={15} /><span>The browser print window will open next. Choose <strong>Save as PDF</strong> as the destination.</span></div></Dialog>
}

const roleOptions = [{ id: 1, name: 'Admin' }, { id: 2, name: 'Manager' }, { id: 3, name: 'User' }]

function TeamAccessPage({ user, notify }: { user: User; notify: (message: string, kind?: Toast['kind']) => void }) {
  const [users, setUsers] = useState<User[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  const [savingId, setSavingId] = useState<number | null>(null)
  const isAdmin = user.role_id === 1
  useEffect(() => {
    if (!isAdmin) { setLoading(false); return }
    api.users().then((result) => setUsers(result.data || [])).catch((requestError) => setError(requestError instanceof Error ? requestError.message : 'Could not load team access users.')).finally(() => setLoading(false))
  }, [isAdmin])
  const updateRole = async (id: number, role_id: number) => {
    setSavingId(id)
    try {
      const result = await api.updateUserRole(id, role_id)
      setUsers((current) => current.map((member) => member.id === id ? result.data : member))
      notify('User role updated')
    } catch (requestError) {
      notify(requestError instanceof Error ? requestError.message : 'Could not update user role.', 'error')
    } finally { setSavingId(null) }
  }
  if (!isAdmin) return <Card><EmptyState title="Admin access required" description="Only administrators can manage team access and user roles." /></Card>
  return <><div className="page-heading"><div><div className="eyebrow">Workspace administration</div><h1>Team access</h1><p>Manage the roles assigned to everyone in this workspace.</p></div><Badge tone="info">Admin only</Badge></div><Card className="table-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Joined</th></tr></thead><tbody>{users.map((member) => <tr key={member.id}><td><div className="td-main"><span className="tiny-avatar">{initials(member.name)}</span><strong>{member.name}</strong>{member.id === user.id && <Badge tone="success">You</Badge>}</div></td><td>{member.email}</td><td><Select value={String(member.role_id || 3)} disabled={savingId === member.id || member.id === user.id} onChange={(event) => updateRole(member.id, Number(event.target.value))} aria-label={`Role for ${member.name}`}>{roleOptions.map((role) => <option key={role.id} value={role.id}>{role.name}</option>)}</Select></td><td>{member.createdAt ? shortDate(member.createdAt) : '—'}</td></tr>)}</tbody></table></div>{loading && <EmptyState title="Loading team access" description="Fetching users from the API…" />}{!loading && error && <EmptyState title="Could not load users" description={error} />}{!loading && !error && !users.length && <EmptyState title="No users found" description="Users will appear here after they register." />}</Card></>
}

function SettingsPage({ language, theme, user, onLanguageChange, onThemeChange, onUserChange, notify }: { language: Language; theme: ThemeMode; user: User; onLanguageChange: (language: Language) => void; onThemeChange: (theme: ThemeMode) => void; onUserChange: (user: User) => void; notify: (message: string, kind?: Toast['kind']) => void }) {
  const [name, setName] = useState(user.name)
  const [email, setEmail] = useState(user.email)
  useEffect(() => { setName(user.name); setEmail(user.email) }, [user])
  const saveProfile = () => { if (!name.trim() || !email.trim()) return; onUserChange({ ...user, name: name.trim(), email: email.trim() }); notify(t(language, 'preferencesSaved')) }
  return <><div className="page-heading"><div><div className="eyebrow">{t(language, 'workspace')}</div><h1>{t(language, 'settings')}</h1><p>Personalize your HTOO workspace and account details.</p></div></div><div className="settings-grid"><Card><div className="settings-card-heading"><div className="settings-icon"><UserCircle size={20} /></div><div><h2>{t(language, 'profile')}</h2><p>{t(language, 'profileDescription')}</p></div></div><div className="form-grid settings-form"><div className="form-field"><label>Name</label><Input value={name} onChange={(event) => setName(event.target.value)} /></div><div className="form-field"><label>Email</label><Input type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></div></div><div className="settings-actions"><Button onClick={saveProfile} disabled={!name.trim() || !email.trim()}><Save size={14} /> {t(language, 'saveChanges')}</Button></div></Card><Card><div className="settings-card-heading"><div className="settings-icon"><Sun size={20} /></div><div><h2>{t(language, 'appearance')}</h2><p>{t(language, 'appearanceDescription')}</p></div></div><div className="preference-row"><div><strong>{t(language, 'theme')}</strong><span>{theme === 'dark' ? t(language, 'dark') : t(language, 'light')}</span></div><div className="segmented-control"><button className={theme === 'light' ? 'selected' : ''} onClick={() => onThemeChange('light')}><Sun size={14} /> {t(language, 'light')}</button><button className={theme === 'dark' ? 'selected' : ''} onClick={() => onThemeChange('dark')}><Moon size={14} /> {t(language, 'dark')}</button></div></div></Card><Card><div className="settings-card-heading"><div className="settings-icon"><span className="language-glyph">文</span></div><div><h2>{t(language, 'language')}</h2><p>{t(language, 'languageDescription')}</p></div></div><div className="preference-row"><div><strong>{t(language, 'language')}</strong><span>{language === 'my' ? t(language, 'myanmar') : t(language, 'english')}</span></div><Select value={language} onChange={(event) => onLanguageChange(event.target.value as Language)} style={{ width: 150 }}><option value="en">{t(language, 'english')}</option><option value="my">{t(language, 'myanmar')}</option></Select></div></Card></div></>
}

function RelatedInvoices({ title, invoices, onView, onEdit, onDelete, onPrint }: { title: string; invoices: Invoice[]; onView: (invoice: Invoice) => void; onEdit: (invoice: Invoice) => void; onDelete: (invoice: Invoice) => Promise<void>; onPrint: (invoice: Invoice) => void }) {
  const [deleteId, setDeleteId] = useState<number | null>(null)
  const invoiceToDelete = invoices.find((invoice) => invoice.id === deleteId)
  return <Card className="table-card"><div className="card-title detail-section-heading"><div><h2>{title}</h2><p>Review every invoice connected to this profile.</p></div><Badge tone="info">{invoices.length} invoices</Badge></div><InvoiceTable invoices={invoices} onView={onView} onEdit={onEdit} onPrint={onPrint} onDelete={(invoice) => setDeleteId(invoice.id)} /><Dialog open={Boolean(invoiceToDelete)} title="Delete invoice?" description="This action will remove the invoice from the live API." onClose={() => setDeleteId(null)} footer={<><Button variant="outline" onClick={() => setDeleteId(null)}>Cancel</Button><Button variant="danger" onClick={async () => { if (invoiceToDelete) await onDelete(invoiceToDelete); setDeleteId(null) }}>Delete invoice</Button></>}><p style={{ color: '#69758a', fontSize: 13 }}>{invoiceToDelete ? `Invoice #${invoiceToDelete.invoice_number} will be permanently removed.` : ''}</p></Dialog></Card>
}

function CustomerDetailPage({ language, customer, cars, invoices, onBack, onViewInvoice, onEditInvoice, onDeleteInvoice, onPrintInvoice }: { language: Language; customer: Customer; cars: Car[]; invoices: Invoice[]; onBack: () => void; onViewInvoice: (invoice: Invoice) => void; onEditInvoice: (invoice: Invoice) => void; onDeleteInvoice: (invoice: Invoice) => Promise<void>; onPrintInvoice: (invoice: Invoice) => void }) {
  const customerCars = cars.filter((car) => car.customer_id === customer.id)
  const customerInvoices = invoices.filter((invoice) => invoice.customer_id === customer.id || invoice.customer?.id === customer.id)
  return <><div className="page-heading"><div><button className="back-link" onClick={onBack}>← {t(language, 'customers')}</button><div className="eyebrow">Customer profile</div><h1>{customer.name}</h1><p>{customer.phone || 'No phone recorded'} · Customer history and related records</p></div></div><div className="detail-grid"><Card><div className="detail-label">{t(language, 'customer')}</div><div className="detail-person"><div className="avatar">{initials(customer.name)}</div><div><h2>{customer.name}</h2><p>{customer.phone || 'No phone recorded'}</p></div></div></Card><Card><div className="detail-label">Vehicles</div><div className="detail-payment"><strong>{customerCars.length}</strong><span>vehicles registered to this customer</span></div></Card><Card><div className="detail-label">Invoices</div><div className="detail-payment"><strong>{customerInvoices.length}</strong><span>invoices in the live records</span></div></Card></div><Card className="table-card"><div className="card-title detail-section-heading"><div><h2>Car summary</h2><p>Vehicles registered under this customer.</p></div><Badge tone="success">{customerCars.length} vehicles</Badge></div>{customerCars.length ? <div className="table-wrap"><table className="data-table"><thead><tr><th>Vehicle</th><th>Make & model</th><th>Service history</th></tr></thead><tbody>{customerCars.map((car) => <tr key={car.id}><td><div className="td-main"><span className="tiny-avatar" style={{ background: '#e8faf4', color: '#1aaf7b' }}><CarFront size={13} /></span><strong>{car.number}</strong></div></td><td>{[car.brand, car.model].filter(Boolean).join(' ') || '—'}</td><td>{customerInvoices.filter((invoice) => invoice.car_id === car.id || invoice.car?.id === car.id).length} invoices</td></tr>)}</tbody></table></div> : <EmptyState title="No vehicles linked" description="Vehicles assigned to this customer will appear here." />}</Card><RelatedInvoices title="Invoice summary" invoices={customerInvoices} onView={onViewInvoice} onEdit={onEditInvoice} onDelete={onDeleteInvoice} onPrint={onPrintInvoice} /></>
}

function CarDetailPage({ language, car, invoices, onBack, onViewInvoice, onEditInvoice, onDeleteInvoice, onPrintInvoice }: { language: Language; car: Car; invoices: Invoice[]; onBack: () => void; onViewInvoice: (invoice: Invoice) => void; onEditInvoice: (invoice: Invoice) => void; onDeleteInvoice: (invoice: Invoice) => Promise<void>; onPrintInvoice: (invoice: Invoice) => void }) {
  const carInvoices = invoices.filter((invoice) => invoice.car_id === car.id || invoice.car?.id === car.id || invoice.car?.number === car.number)
  return <><div className="page-heading"><div><button className="back-link" onClick={onBack}>← {t(language, 'vehicles')}</button><div className="eyebrow">Vehicle profile</div><h1>{car.number}</h1><p>{[car.brand, car.model].filter(Boolean).join(' ') || 'Vehicle history'} · Related service records</p></div></div><div className="detail-grid"><Card><div className="detail-label">{t(language, 'vehicle')}</div><div className="detail-person"><div className="vehicle-icon"><CarFront size={19} /></div><div><h2>{car.number}</h2><p>{[car.brand, car.model].filter(Boolean).join(' ') || 'No model recorded'}</p></div></div></Card><Card><div className="detail-label">Owner</div><div className="detail-person"><div className="avatar">{initials(car.customer?.name || 'NA')}</div><div><h2>{car.customer?.name || '—'}</h2><p>Registered customer</p></div></div></Card><Card><div className="detail-label">Invoices</div><div className="detail-payment"><strong>{carInvoices.length}</strong><span>service invoices for this vehicle</span></div></Card></div><RelatedInvoices title="Invoice summary" invoices={carInvoices} onView={onViewInvoice} onEdit={onEditInvoice} onDelete={onDeleteInvoice} onPrint={onPrintInvoice} /></>
}

function CustomersPage({ customers, onView, onChange, notify }: { customers: Customer[]; onView: (customer: Customer) => void; onChange: (value: Customer[]) => void; notify: (message: string, kind?: Toast['kind']) => void }) {
  const [query, setQuery] = useState(''); const [page, setPage] = useState(1); const [dialog, setDialog] = useState<'new' | Customer | null>(null); const [deleteId, setDeleteId] = useState<number | null>(null); const pageSize = 6; const filtered = customers.filter((customer) => `${customer.name} ${customer.phone || ''}`.toLowerCase().includes(query.toLowerCase())); const pages = Math.max(1, Math.ceil(filtered.length / pageSize)); const rows = filtered.slice((page - 1) * pageSize, page * pageSize); useEffect(() => setPage(1), [query])
  const save = async (name: string) => { try { const result = typeof dialog === 'object' && dialog ? await api.updateCustomer(dialog.id, { name }) : await api.createCustomer({ name }); onChange(typeof dialog === 'object' && dialog ? customers.map((customer) => customer.id === dialog.id ? { ...customer, ...result.data } : customer) : [{ ...result.data, _count: { invoices: 0, cars: 0 } }, ...customers]); setDialog(null); notify(typeof dialog === 'object' ? 'Customer updated' : 'Customer added') } catch (requestError) { notify(requestError instanceof Error ? requestError.message : 'Could not save customer to the live API.', 'error') } }
  const remove = async () => { if (!deleteId) return; try { await api.deleteCustomer(deleteId); onChange(customers.filter((customer) => customer.id !== deleteId)); setDeleteId(null); notify('Customer deleted') } catch (requestError) { notify(requestError instanceof Error ? requestError.message : 'Could not delete customer from the live API.', 'error') } }
  return <><ListHeading eyebrow="Customer directory" title="Customers" description="Manage the people and businesses you serve." actionLabel="Add customer" onAction={() => setDialog('new')} /><div className="toolbar"><div className="toolbar-left"><div className="search-box"><Search size={15} /><Input placeholder="Search customers…" value={query} onChange={(e) => setQuery(e.target.value)} /></div></div><span style={{ fontSize: 11, color: '#8892a4' }}>{filtered.length} customers</span></div><Card className="table-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>Customer</th><th>Phone</th><th>Vehicles</th><th>Invoices</th><th></th></tr></thead><tbody>{rows.map((customer) => <tr key={customer.id}><td><div className="td-main"><span className="tiny-avatar">{initials(customer.name)}</span><strong>{customer.name}</strong></div></td><td>{customer.phone || '—'}</td><td>{customer._count?.cars || 0}</td><td>{customer._count?.invoices || 0}</td><td><div className="table-actions"><Button variant="ghost" size="icon" title="View" onClick={() => onView(customer)}><Eye size={14} /></Button><Button variant="ghost" size="icon" title="Edit" onClick={() => setDialog(customer)}><Pencil size={14} /></Button><Button variant="ghost" size="icon" title="Delete" onClick={() => setDeleteId(customer.id)}><Trash2 size={14} /></Button></div></td></tr>)}</tbody></table></div>{!rows.length && <EmptyState title="No customers found" description="Try another search term." />}<Pagination page={page} pages={pages} total={filtered.length} pageSize={pageSize} onPage={setPage} /></Card><CustomerDialog open={Boolean(dialog)} initial={typeof dialog === 'object' ? dialog : null} onClose={() => setDialog(null)} onSave={save} /><Dialog open={Boolean(deleteId)} title="Delete customer?" description="Customers with linked invoices cannot be removed by the API." onClose={() => setDeleteId(null)} footer={<><Button variant="outline" onClick={() => setDeleteId(null)}>Cancel</Button><Button variant="danger" onClick={remove}>Delete customer</Button></>}><p style={{ color: '#69758a', fontSize: 13 }}>This will remove the customer profile from this workspace.</p></Dialog></>
}
function CustomerDialog({ open, initial, onClose, onSave }: { open: boolean; initial: Customer | null; onClose: () => void; onSave: (name: string) => void }) { const [name, setName] = useState(initial?.name || ''); useEffect(() => setName(initial?.name || ''), [initial, open]); return <Dialog open={open} title={initial ? 'Edit customer' : 'Add customer'} description="Keep customer contact records easy to find." onClose={onClose} footer={<><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={() => onSave(name)} disabled={!name.trim()}>Save customer</Button></>}><div className="form-field"><label>Customer or company name</label><Input autoFocus value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Golden Land Logistics" /></div></Dialog> }

function CarsPage({ cars, customers, onView, onChange, notify }: { cars: Car[]; customers: Customer[]; onView: (car: Car) => void; onChange: (value: Car[]) => void; notify: (message: string, kind?: Toast['kind']) => void }) {
  const [query, setQuery] = useState(''); const [page, setPage] = useState(1); const [dialog, setDialog] = useState<'new' | Car | null>(null); const [deleteId, setDeleteId] = useState<number | null>(null); const pageSize = 6; const filtered = cars.filter((car) => `${car.number} ${car.brand} ${car.model} ${car.customer?.name}`.toLowerCase().includes(query.toLowerCase())); const pages = Math.max(1, Math.ceil(filtered.length / pageSize)); const rows = filtered.slice((page - 1) * pageSize, page * pageSize); useEffect(() => setPage(1), [query])
  const save = async (data: CarForm) => { const payload = { number: data.number, brand: data.brand, model: data.model, customer_id: Number(data.customer_id) }; try { const result = typeof dialog === 'object' && dialog ? await api.updateCar(dialog.id, payload) : await api.createCar(payload); onChange(typeof dialog === 'object' && dialog ? cars.map((car) => car.id === dialog.id ? { ...car, ...result.data, customer: customers.find((customer) => customer.id === payload.customer_id) } : car) : [{ ...result.data, customer: customers.find((customer) => customer.id === payload.customer_id) }, ...cars]); setDialog(null); notify(typeof dialog === 'object' ? 'Vehicle updated' : 'Vehicle added') } catch (requestError) { notify(requestError instanceof Error ? requestError.message : 'Could not save vehicle to the live API.', 'error') } }
  const remove = async () => { if (!deleteId) return; try { await api.deleteCar(deleteId); onChange(cars.filter((car) => car.id !== deleteId)); setDeleteId(null); notify('Vehicle deleted') } catch (requestError) { notify(requestError instanceof Error ? requestError.message : 'Could not delete vehicle from the live API.', 'error') } }
  return <><ListHeading eyebrow="Fleet directory" title="Vehicles" description="Keep every truck profile ready for its next service." actionLabel="Add vehicle" onAction={() => setDialog('new')} /><div className="toolbar"><div className="search-box"><Search size={15} /><Input placeholder="Search number, brand, customer…" value={query} onChange={(e) => setQuery(e.target.value)} /></div><span style={{ fontSize: 11, color: '#8892a4' }}>{filtered.length} vehicles</span></div><Card className="table-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>Vehicle</th><th>Make & model</th><th>Owner</th><th>Service status</th><th></th></tr></thead><tbody>{rows.map((car) => <tr key={car.id}><td><div className="td-main"><span className="tiny-avatar" style={{ background: '#e8faf4', color: '#1aaf7b' }}><CarFront size={13} /></span><strong>{car.number}</strong></div></td><td>{[car.brand, car.model].filter(Boolean).join(' ') || '—'}</td><td>{car.customer?.name || customers.find((customer) => customer.id === car.customer_id)?.name || '—'}</td><td><Badge tone="success">active profile</Badge></td><td><div className="table-actions"><Button variant="ghost" size="icon" title="View" onClick={() => onView(car)}><Eye size={14} /></Button><Button variant="ghost" size="icon" title="Edit" onClick={() => setDialog(car)}><Pencil size={14} /></Button><Button variant="ghost" size="icon" title="Delete" onClick={() => setDeleteId(car.id)}><Trash2 size={14} /></Button></div></td></tr>)}</tbody></table></div>{!rows.length && <EmptyState title="No vehicles found" description="Try another search term." />}<Pagination page={page} pages={pages} total={filtered.length} pageSize={pageSize} onPage={setPage} /></Card><CarDialog open={Boolean(dialog)} initial={typeof dialog === 'object' ? dialog : null} customers={customers} onClose={() => setDialog(null)} onSave={save} /><Dialog open={Boolean(deleteId)} title="Delete vehicle?" description="Vehicles with linked invoices cannot be deleted." onClose={() => setDeleteId(null)} footer={<><Button variant="outline" onClick={() => setDeleteId(null)}>Cancel</Button><Button variant="danger" onClick={remove}>Delete vehicle</Button></>}><p style={{ color: '#69758a', fontSize: 13 }}>This will remove the vehicle profile from the fleet directory.</p></Dialog></>
}
type CarForm = { number: string; brand: string; model: string; customer_id: string }
function CarDialog({ open, initial, customers, onClose, onSave }: { open: boolean; initial: Car | null; customers: Customer[]; onClose: () => void; onSave: (data: CarForm) => void }) { const [form, setForm] = useState<CarForm>({ number: initial?.number || '', brand: initial?.brand || '', model: initial?.model || '', customer_id: String(initial?.customer_id || customers[0]?.id || '') }); useEffect(() => setForm({ number: initial?.number || '', brand: initial?.brand || '', model: initial?.model || '', customer_id: String(initial?.customer_id || customers[0]?.id || '') }), [initial, open, customers]); const patch = (next: Partial<CarForm>) => setForm((value) => ({ ...value, ...next })); return <Dialog open={open} title={initial ? 'Edit vehicle' : 'Add vehicle'} description="Add the registration and owner details for a vehicle." onClose={onClose} footer={<><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={() => onSave(form)} disabled={!form.number || !form.customer_id}>Save vehicle</Button></>}><div className="form-grid"><div className="form-field"><label>Vehicle number</label><Input autoFocus value={form.number} onChange={(e) => patch({ number: e.target.value })} placeholder="9H-4821" /></div><div className="form-field"><label>Customer</label><Select value={form.customer_id} onChange={(e) => patch({ customer_id: e.target.value })}>{customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.name}</option>)}</Select></div><div className="form-field"><label>Brand</label><Input value={form.brand} onChange={(e) => patch({ brand: e.target.value })} placeholder="Hino" /></div><div className="form-field"><label>Model</label><Input value={form.model} onChange={(e) => patch({ model: e.target.value })} placeholder="500 Series" /></div></div></Dialog> }

function ItemDetailPage({ language, item, invoices, onBack, onViewInvoice }: { language: Language; item: Item; invoices: Invoice[]; onBack: () => void; onViewInvoice: (invoice: Invoice) => void }) {
  const invoiceRows = invoices.flatMap((invoice) => invoice.details?.filter((detail) => detail.item_id === item.id || detail.item?.id === item.id || detail.item?.name === item.name || detail.name === item.name).map((detail) => ({ invoice, detail })) || [])
  return <><div className="page-heading"><div><button className="back-link" onClick={onBack}>← {t(language, 'services')}</button><div className="eyebrow">Service item profile</div><h1>{item.name}</h1><p>Invoice usage history with date, quantity, and price details.</p></div></div><div className="detail-grid"><Card><div className="detail-label">Service item</div><div className="detail-person"><div className="vehicle-icon" style={{ background: '#fff6dc', color: '#d39d1f' }}><ClipboardList size={19} /></div><div><h2>{item.name}</h2><p>Current listed price: {money(item.price)}</p></div></div></Card><Card><div className="detail-label">Invoices</div><div className="detail-payment"><strong>{invoiceRows.length}</strong><span>invoice line items using this service</span></div></Card><Card><div className="detail-label">Quantity</div><div className="detail-payment"><strong>{invoiceRows.reduce((sum, row) => sum + Number(row.detail.quantity || 0), 0)}</strong><span>total units recorded</span></div></Card></div><Card className="table-card"><div className="card-title detail-section-heading"><div><h2>Invoice summary</h2><p>Date, quantity, and price for every invoice using this item.</p></div><Badge tone="info">{invoiceRows.length} records</Badge></div>{invoiceRows.length ? <div className="table-wrap"><table className="data-table"><thead><tr><th>Invoice</th><th>Date</th><th>Customer</th><th>Vehicle</th><th>Quantity</th><th>Price</th><th>Total</th><th></th></tr></thead><tbody>{invoiceRows.map(({ invoice, detail }, index) => <tr key={`${invoice.id}-${detail.id || index}`}><td><button className="table-link" onClick={() => onViewInvoice(invoice)}>#{invoice.invoice_number}</button></td><td>{shortDate(invoice.date)}</td><td>{invoice.customer?.name || '—'}</td><td>{invoice.car?.number || '—'}</td><td>{detail.quantity}</td><td>{money(detail.price)}</td><td><strong>{money(Number(detail.price) * Number(detail.quantity))}</strong></td><td><Button variant="ghost" size="icon" title="View invoice" onClick={() => onViewInvoice(invoice)}><Eye size={14} /></Button></td></tr>)}</tbody></table></div> : <EmptyState title="No invoice usage found" description="This item has not been used on an invoice yet." />}</Card></>
}

function ItemsPage({ items, invoices, onView }: { items: Item[]; invoices: Invoice[]; onView: (item: Item) => void }) { const [query, setQuery] = useState(''); const filtered = items.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); return <><div className="page-heading"><div><div className="eyebrow">Service catalog</div><h1>Services & items</h1><p>Services shown here are read from the live invoice data.</p></div><span className="live-label">Live API</span></div><div className="toolbar"><div className="search-box"><Search size={15} /><Input placeholder="Search services…" value={query} onChange={(e) => setQuery(e.target.value)} /></div><span style={{ fontSize: 11, color: '#8892a4' }}>{filtered.length} services</span></div><Card className="table-card"><div className="table-wrap"><table className="data-table"><thead><tr><th>Service</th><th>Latest price</th><th>Usage</th><th></th></tr></thead><tbody>{filtered.map((item) => <tr key={item.id}><td><div className="td-main"><span className="tiny-avatar" style={{ background: '#fff6dc', color: '#d39d1f' }}><ClipboardList size={13} /></span><strong>{item.name}</strong></div></td><td>{money(item.price)}</td><td>{invoices.reduce((count, invoice) => count + (invoice.details?.some((detail) => detail.item_id === item.id || detail.item?.id === item.id || detail.item?.name === item.name) ? 1 : 0), 0)} invoices</td><td><Button variant="ghost" size="icon" title="View" onClick={() => onView(item)}><Eye size={14} /></Button></td></tr>)}</tbody></table></div>{!filtered.length && <EmptyState title="No services found" description="Services will appear here when they are returned by the live API." />}<div className="pagination"><span>Showing {filtered.length} services from live invoices</span></div></Card></> }
function ListHeading({ eyebrow, title, description, actionLabel, onAction }: { eyebrow: string; title: string; description: string; actionLabel: string; onAction: () => void }) { return <div className="page-heading"><div><div className="eyebrow">{eyebrow}</div><h1>{title}</h1><p>{description}</p></div><div className="actions"><Button onClick={onAction}><Plus size={15} /> {actionLabel}</Button></div></div> }
function Pagination({ page, pages, total, pageSize, onPage }: { page: number; pages: number; total: number; pageSize: number; onPage: (page: number) => void }) { return <div className="pagination"><span>Showing {total ? (page - 1) * pageSize + 1 : 0}–{Math.min(page * pageSize, total)} of {total}</span><div className="pagination-actions"><button disabled={page <= 1} onClick={() => onPage(page - 1)}><ChevronLeft size={14} /></button>{Array.from({ length: pages }, (_, index) => index + 1).slice(Math.max(0, page - 2), Math.min(pages, page + 1)).map((number) => <button className={number === page ? 'active' : ''} key={number} onClick={() => onPage(number)}>{number}</button>)}<button disabled={page >= pages} onClick={() => onPage(page + 1)}><ChevronRight size={14} /></button></div></div> }

function PrintInvoice({ invoice, options }: { invoice: Invoice; options: PrintOptions }) { return <div className={`print-only print-${options.design}`}><div className="print-invoice-head" style={{ borderColor: options.accent }}><div><div className="print-brand" style={{ color: options.accent }}>{options.header || 'HTOO Heavy Vehicle'}</div><h1>Service invoice</h1><p>{options.subtitle}{options.address ? ` · ${options.address}` : ''}{options.phone ? ` · ${options.phone}` : ''}</p></div><div style={{ textAlign: 'right' }}><strong>#{invoice.invoice_number}</strong><p>{shortDate(invoice.date)}</p></div></div><div className="print-parties"><div><small>Bill to</small><strong>{invoice.customer?.name}</strong><span>{invoice.customer?.phone || ''}</span></div><div><small>Vehicle</small><strong>{invoice.car?.number}</strong><span>{[invoice.car?.brand, invoice.car?.model].filter(Boolean).join(' ')}</span></div><div><small>Payment</small><strong style={{ color: options.accent }}>{normalizePaymentType(invoice.payment_type)}</strong><span>{shortDate(invoice.payment_date)}</span></div></div><table><thead><tr><th>Service description</th><th>Qty</th><th>Price</th><th>Total</th></tr></thead><tbody>{invoice.details?.map((detail, index) => <tr key={index}><td>{detail.item?.name || detail.name}</td><td>{detail.quantity}</td><td>{money(detail.price)}</td><td>{money(Number(detail.price) * detail.quantity)}</td></tr>)}</tbody></table><div className="print-summary"><div><span>Subtotal</span><span>{money(invoice.sub_total)}</span></div><div><span>Advance</span><span>{money(invoice.advance)}</span></div><div className="grand" style={{ borderColor: options.accent }}><strong>Grand total</strong><strong>{money(invoice.grand_total)}</strong></div></div></div> }

export default App
